class Solution(object):
    def intersect(self, nums1, nums2):
        output = []
        for item in nums1:
            if item in nums2:
                output.append(item)
                nums2.remove(item)
        return output